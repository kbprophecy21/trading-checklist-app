/*
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import HomeScreen from './screens/HomeScreen';
import TakeTradeScreen from './screens/TakeTradeScreen';
import ManageTradeScreen from './screens/ManageTradeScreen';
import ExitTradeScreen from './screens/ExitTradeScreen';

const Stack = createStackNavigator();
const App = () => {
  return (
    <NavigationContainer>
      <Stack.Navigator initialRouteName="Home">
        <Stack.Screen name="HomeScreen" component={HomeScreen} />
        <Stack.Screen name="TakeTrade" component={TakeTradeScreen} />
        <Stack.Screen name="ManageTrade" component={ManageTradeScreen} />
        <Stack.Screen name="ExitTrade" component={ExitTradeScreen} />
      </Stack.Navigator>
    </NavigationContainer>
  );
};



export default App; */
